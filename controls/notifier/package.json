{
  "name": "notifier",
  "version": "0.1.0",
  "description": "In-Room Control that lets users set and update an email address, and sends Webex Teams notifications to this address via a bot.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Stève Sfartz <stsfartz@cisco.com>",
  "license": "MIT",
  "dependencies": {
    "jsxapi": "^4.1.3",
    "request": "^2.87.0"
  }
}
